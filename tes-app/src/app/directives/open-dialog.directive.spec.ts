import { OpenDialogDirective } from './open-dialog.directive';

describe('OpenDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDialogDirective();
    expect(directive).toBeTruthy();
  });
});
